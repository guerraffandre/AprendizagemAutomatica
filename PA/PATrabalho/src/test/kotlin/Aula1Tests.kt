/*import kotlin.test.Test
import kotlin.test.assertFalse
import kotlin.test.assertTrue

class Tests {

    @Test
    fun `positive case`() {
        //val não pode ser alterado
        //var pode ser alterado
        var list = listOf(1, 2, 3, 4)
        val isOrd = isOrdered(list, true)
        assertTrue(isOrd)
    }

    @Test
    fun `negative case`() {
        var list = listOf(1, 2, 3, 5, 4, -1)
        val isOrd = isOrdered(list, false)
        assertFalse(isOrd)
    }
}*/